// Printing Array elements with Comma delimiters
// Parameters: an array of any size. elements may be string or int
// Return: string with comma delimited element of the array in same order
// Example: input - printArray(["h","o","l","a"]) return - h,o,l,a
// Pseudo Code: can just use an array method to convert to string, with no arguments


function printArray(array){
    return array.toString()
  }

  printArray(["h","o","l","a"])